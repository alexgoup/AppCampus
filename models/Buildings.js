var mongoose = require('mongoose');

/*var BuildingSchema = new mongoose.Schema({
  title: String,
  cost: Number
});

var Building = mongoose.model('Building', BuildingSchema);

var sc = new Building({ name: 'Student Center' });
console.log(sc.name); */